@{
    // This view should not be rendered as the Index action redirects to Login
    // Kept as a fallback in case view is accessed directly
    Layout = null;
    ViewData["Title"] = "Home";
}

<p>Redirecting to login...</p>
<script>
    window.location.href = '@Url.Action("Login", "Home")';
</script>
